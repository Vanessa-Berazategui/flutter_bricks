name: presentation_layer_feature
description: A presentation flavoured feature brick.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.35 <0.1.0"

vars:
  name:
    type: string
    description: Presentation layer feature
    default: <feature_name>
    prompt: What is the Presentation Layer Feature's name?
