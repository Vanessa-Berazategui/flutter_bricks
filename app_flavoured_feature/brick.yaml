name: app_flavoured_feature
description: An app flavoured feature brick.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.35 <0.1.0"

vars:
  name:
    type: string
    description: App feature name
    default: <feature_name>
    prompt: What is the App Feature's name?
